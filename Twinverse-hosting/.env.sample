# Twinverse Hosting Configuration
# Rename to .env and update values before deployment

# Image tag to use (e.g., latest, 1.0.0, etc.)
IMAGE_TAG=latest

# Host address for API URLs (use domain or IP)
HOST_ADDRESS=localhost

# API Keys (required)
GEMINI_API_KEY=your_gemini_api_key_here

# Ports (optional - change if you have port conflicts)
FRONTEND_PORT=3000
SIMULATED_API_PORT=8007
MOVABLE_API_PORT=8000
INVENTORY_API_PORT=8002
BACKEND_API_PORT=9000
WHATSAPP_PORT=3001